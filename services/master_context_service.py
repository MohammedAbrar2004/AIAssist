def get_master_context() -> str:
    # [PLACEHOLDER] Replace with actual universal EHS master context:
    # field descriptions, domain terminology, field type semantics, regulatory references, etc.
    return (
        "[PLACEHOLDER: Universal EHS master context — "
        "field descriptions, domain terminology, and field type semantics go here]"
    )
